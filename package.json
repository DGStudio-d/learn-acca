{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build",
        "dev": "vite",
        "test": "echo 'Frontend tests not configured yet' && exit 0",
        "test:unit": "echo 'Frontend unit tests not configured yet' && exit 0",
        "lint": "echo 'Frontend linting not configured yet' && exit 0",
        "type-check": "echo 'TypeScript type checking not configured yet' && exit 0"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.4"
    }
}
